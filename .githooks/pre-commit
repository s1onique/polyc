#!/bin/sh
# .githooks/pre-commit
#
# Thin wrapper that delegates to scripts/quality/gate-fast.sh.
# Gate logic lives in the script; this file is only the trigger.

set -eu

exec ./scripts/quality/gate-fast.sh
