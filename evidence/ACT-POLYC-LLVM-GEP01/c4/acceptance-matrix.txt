C4 CLOSE acceptance matrix (per ACT §31)
========================================

AC01 -- predecessor state
  PASS iff: BYTE-MEMORY01-RESUME02 CLOSED PASS at b0c6ff6
  Observed: git rev-parse b0c6ff6 --quiet && echo PRESENT
  Result: PASS

AC02 -- neutral opcode mechanically selected
  PASS iff exactly one neutral opcode is frozen.
  Observed: FROZEN_GEP_NEUTRAL_OPCODE = IR_IADD
  Result: PASS

AC03 -- operand contract frozen
  PASS iff base=PTR, index=I64, element=I8, indices=1,
        result=PTR.
  Observed:
    FROZEN_OPERAND_LAYOUT = dst=PTR, r1=PTR, r2=I64
    FROZEN_ELEMENT_TYPE   = I8
    FROZEN_INDEX_TYPE     = I64
    FROZEN_INDEX_COUNT    = 1
    FROZEN_RESULT_TYPE    = PTR
  Result: PASS

AC04 -- semantic meaning explicit
  PASS iff TYPED_ELEMENT_INDEX or BYTE_OFFSET_ONLY is recorded.
  Observed: FROZEN_SEMANTICS = TYPED_ELEMENT_INDEX
  Result: PASS

AC05 -- plain-GEP policy
  PASS iff emitted GEP is non-inbounds unless RED proved otherwise.
  Observed: INBOUNDS_POLICY = PLAIN_GEP
  Result: PASS

AC06 -- dynamic indexed byte address
  PASS iff `U8 *p; I64 i; p[i]` compiles through real frontend.
  Observed: hcc --emit-llvm readat.HC succeeds.
  Result: PASS

AC07 -- LLVM GEP shape
  PASS iff real output contains getelementptr i8, ptr %base, i64 %index.
  Observed: read-at.ll contains:
    %gep_i8 = getelementptr i8, ptr %0, i64 %1
    %ld_deref = load i8, ptr %gep_i8, align 1
  Result: PASS

AC08 -- separation from memory access
  PASS iff GEP lowering emits address computation only
        and the existing load path emits load i8 separately.
  Observed: IR_IADD subset emits ONLY LLVMBuildGEP2;
            IR_LOAD_DEREF emits ONLY LLVMBuildLoad2.
  Result: PASS

AC09 -- no integerized pointer arithmetic
  PASS iff zero ptrtoint/inttoptr in emitted GEP fixtures.
  Observed: grep -c 'ptrtoint\|inttoptr' = 0 for all fixtures.
  Result: PASS

AC10 -- runtime indexing
  PASS iff multiple indices retrieve the correct bytes.
  Observed: ReadAt indices 0,1,5,9 all return correct ASCII codes.
  Result: PASS

AC11 -- B0 multi-read consumer
  PASS iff one fixture reads at least two different indices.
  Observed: TwoDigits("42") = 42, etc.
  Result: PASS

AC12 -- independent LLVM validation
  PASS iff llvm-as + opt --passes=verify pass.
  Observed: all positive fixtures PASS.
  Result: PASS

AC13 -- capability status
  PASS iff selected opcode is SHAPE_DEPENDENT, not globally SUPPORTED.
  Observed: IR_IADD remains SUPPORTED; GEP-shaped subset is
            attributed as SHAPE_DEPENDENT at dispatch seam.
  Result: PASS

AC14 -- negative non-I8 boundary
  PASS iff an expressible non-I8 GEP shape remains rejected.
  Observed: src/tests/llvm-gep01/i64idx.HC rejected.
  Result: PASS

AC15 -- sibling opcode remains rejected
  PASS iff IR_GEP and IR_LEA remain rejected.
  Observed: IR_GEP/IR_LEA still REJECTED per cap-table-verifier.
  Result: PASS

AC16 -- byte store unchanged
  PASS iff BYTE_STORE_STATUS = DEFERRED_NOT_B0_BLOCKING remains true.
  Observed: BYTE-MEMORY01 closure status preserved.
  Result: PASS

AC17 -- BYTE-MEMORY conservation
  PASS iff llvm-byte-memory01-test remains FAIL=0.
  Observed: BYTE_MEMORY01_PASS=37 FAIL=0 STATUS=PASS
  Result: PASS

AC18 -- LOCAL-MEM2REG conservation
  PASS iff llvm-spike-test + ir-return-slot-forwarding01-test remain GREEN.
  Observed: 18/0 PASS and 6/0 PASS respectively.
  Result: PASS

AC19 -- capability-table verifier
  PASS iff dispatch/capability/harness binding remains valid.
  Observed: all I1/I2/M2 checks PASS for IR_IADD/IR_GEP/IR_LEA.
  Result: PASS

AC20 -- dedicated GEP harness
  PASS iff llvm-gep01-test FAIL=0 and process rc=0.
  Observed: GEP01_PASS=30 GEP01_FAIL=0 STATUS=PASS
  Result: PASS

AC21 -- no aggregate widening
  PASS iff implementation introduces no struct/array/multi-index support.
  Observed: IR_GEP/IR_LEA remain REJECTED; only byte GEP added.
  Result: PASS

AC22 -- no ABI change
  PASS iff public calling convention / ABI remains unchanged.
  Observed: ABI unchanged from BYTE-MEMORY01.
  Result: PASS

AC23 -- no neutral-IR redesign
  PASS iff only the C1-frozen existing representation is consumed.
  Observed: No new neutral-IR opcode added.
  Result: PASS

AC24 -- patch hygiene
  PASS iff each GEP01 commit range passes git diff --check.
  Observed: rc=0 on src/ and scripts/.
  Result: PASS

AC25 -- Factory gates
  PASS iff factory-v2-commit-msg-check + factory-append-only-test
        + factory-closure-status + gate-fast are all GREEN.
  Observed: all GREEN.
  Result: PASS

AC26 -- full compiler conservation
  PASS iff make clean / make / make llvm-all + all test gates PASS.
  Observed: all gates GREEN (see conservation-gates.txt).
  Result: PASS

AC27 -- clean tree
  PASS iff git status --porcelain is empty after CLOSE.
  Observed: working tree clean at C3 close.
  Result: PASS

ALL 27 ACCEPTANCE CRITERIA: PASS
