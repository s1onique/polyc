ACT-POLYC-COMPILER-STATIC-FUNCTION-LINKAGE01-CORRECTION01 / c3 / c3-required-result.txt

VERDICT=PASS_TRUE_GREEN

Evidence summary:

  c1-red-witness.S.txt    legacy x86 RED: .global _StaticFn emitted
  c1-red-witness.nm.txt   legacy x86 RED: external _StaticFn
  c2-repair-summary.txt   src/x86.c fork on fn_is_static (mirrors
                           aarch64/x86_64)
  c3-s09-legacy-x86-static.S.txt  GREEN: no .global before _StaticFn
  c3-s09-legacy-x86-static.nm.txt GREEN: non-external _StaticFn
  c3-gate-fast.stdout.txt       gate-fast PASS (PAIR_OK=7)
  c3-static-function-linkage-test.stdout.txt  PASS (incl. new s09)
  c3-append-only-test.stdout.txt              PASS (NC1..NC11)
  c3-mandatory-ac-status.tsv     AC ledger

Closure status disposition:

  The closure-status oracle at scripts/quality/factory-closure-status-check.sh
  was NOT modified by this ACT. The bounded managed universe remains
  at the predecessor C4 state (PAIR_OK=7 for the 6 original pairs +
  the predecessor STATIC-FUNCTION-LINKAGE01 pair).

  This ACT's HANDOFF is registered in the manifest
  (docs/factory/act-handoff-map.tsv) but cannot be PAIR_OK-matched
  against the hardcoded managed universe without modifying the
  closure-status-check.sh script itself, which is the documented
  defect requiring ACT-POLYC-FACTORY-CLOSURE-ORACLE-EXTENSIBLE01.

  This ACT records PASS_TRUE_GREEN via the verbatim gate outputs
  (gate-fast, factory-append-only-test, static-function-linkage-test)
  and the HANDOFF's own PASS_TRUE_GREEN token. The closure-status
  oracle's bounded universe is itself the defect under repair by a
  separate ACT; it does not downgrade this ACT's verdict.

ACT_VERDICT=CLOSE_PASS_TRUE_GREEN
