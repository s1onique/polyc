ACT-POLYC-TOOLING-RUNTIME01
C3 EVIDENCE — GEP dogfood slice parity

------------------------------------------------------------
Slice under test
------------------------------------------------------------

Read-only assertion slice of scripts/quality/
llvm-gep01-test.sh limited to the readat fixture:

  - compile src/tests/llvm-gep01/readat.HC via
    hcc --emit-llvm
  - hcc exit code == 0
  - emitted IR contains "getelementptr i8"
  - emitted IR does NOT contain "getelementptr inbounds"
  - emitted IR does NOT contain "ptrtoint"
  - emitted IR does NOT contain "inttoptr"

The existing llvm-gep01-test.sh remains authoritative
during this ACT; this slice is the parity witness per
ACT §23 / AC23.

------------------------------------------------------------
Bash harness authoritative result
------------------------------------------------------------

A standalone Bash reproduction of the slice (mirror of
llvm-gep01-test.sh logic, no shared state):

  $ /tmp/llvm-gep-readat-bash-check.sh
  LLVM backend capability contract: ok (56 rows, ...)
  CAPABILITY_COUNTERS supported=2 rejected=0 ...
  BASH-RESULT: PASS

------------------------------------------------------------
PolyC self-test result
------------------------------------------------------------

  $ /tmp/runtime01-selftest \
      --child=/tmp/rt01-child \
      --hcc=$(pwd)/hcc \
      --llvm-install-dir=$(pwd)/build/test-prefix

  PASS  GEP hcc-exit
  PASS  GEP contains-gep-i8
  PASS  GEP no-inbounds
  PASS  GEP no-ptrtoint
  PASS  GEP no-inttoptr
  ----
  RT01_PASS=18
  RT01_FAIL=0
  STATUS=PASS
  exit=0

------------------------------------------------------------
Parity verdict
------------------------------------------------------------

  Bash harness slice: PASS
  PolyC self-test:    PASS (5/5 GEP assertions)
  Parity:             YES

------------------------------------------------------------
Notes
------------------------------------------------------------

- The PolyC test spawns hcc directly via SpawnAndCapture
  with explicit argv. No /bin/sh -c is involved.
- stdout / stderr of the spawned hcc are captured
  separately; in this run both are empty for hcc's
  normal output path.
- The emitted IR is read back via FileRead and asserted
  via Contains / negation patterns.

End of file.
