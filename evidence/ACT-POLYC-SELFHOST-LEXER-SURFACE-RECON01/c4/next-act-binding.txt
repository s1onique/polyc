ACT-POLYC-SELFHOST-LEXER-SURFACE-RECON01 — C4 — next ACT binding
================================================================

NEXT_ACT      = ACT-POLYC-SELFHOST-LEXER02
LEXER02_SCOPE = EXACTLY (scalar_literal_scanner)

Specifically, LEXER02 is authorized to:
  - Create tools/bootstrap/selfhost-lexer-scalar-literal.HC
    implementing a PolyC component named BootstrapScanScalarLiteral
    (or equivalent ABI name) with the ABI specified in
    evidence/ACT-POLYC-SELFHOST-LEXER-SURFACE-RECON01/c4/final-winner.txt
    (4 inputs, 7 outputs).
  - Add a row to docs/factory/SELF-HOST-COMPONENTS.tsv for the
    new component.
  - Modify src/lexer.c (under -DHCC_USE_SELFHOST_COMPONENTS) to
    delegate the lexCore numeric ('0'..'9') and char_const ('\'')
    cases to the PolyC component, with the legacy C path preserved
    for stage0 builds.
  - Update src/CMakeLists.txt to add the necessary FATAL_ERROR
    guards for stage1/2/3 (mirroring the existing operator guard).
  - Create oracle/seam-runner test fixtures under tools/quality/.

LEXER02 is NOT authorized to:
  - Migrate string literal scanning (lexString) — E5 ineligible.
  - Migrate the keyword-classification Map* lookup — E5 ineligible.
  - Modify parser, AST, IR, backend, runtime.
  - Add Python source or Python execution edges.
  - Begin S2 (parser) self-host work.

The ACT-POLYC-SELFHOST-LEXER-SURFACE-RECON01 recon ACT stops at
this binding (per ACT §72 hard stop). LEXER02 is the next ACT and
must open with its own C1 RED phase against the LEXER02 scope.
