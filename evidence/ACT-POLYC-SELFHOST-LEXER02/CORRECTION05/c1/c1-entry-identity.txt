ACT-POLYC-SELFHOST-LEXER02-CORRECTION05 — C1 RED — entry identity
==================================================================

Recorded at session start (F1):

  $ git branch --show-current
  main

  $ git status --short
  (clean at session start; this commit adds the new
   evidence/ACT-POLYC-SELFHOST-LEXER02/CORRECTION05/c1/
   directory and the ACT authorization artifact
   docs/acts/ACT-POLYC-SELFHOST-LEXER02-CORRECTION05.md)

  $ git rev-parse HEAD
  58a89cc052a46a79d0c71feff41fe5573fe0f166

  $ git log -1 --no-patch --format='%s'
  ACT-POLYC-SELFHOST-LEXER02-CORRECTION04 C4 CLOSE

Entry identity verified at `58a89cc` (the closing commit of
ACT-POLYC-SELFHOST-LEXER02-CORRECTION04, which is the
authorized predecessor of CORRECTION05). The reviewer verdict
on `58a89cc` is `CORRECTION04_CLOSE_PASS=FALSE_GREEN`, and
the seven closure-truth defects enumerated in
`docs/acts/ACT-POLYC-SELFHOST-LEXER02-CORRECTION05.md` §1
are mechanically observable from this commit.

Worktree state at C1 entry: clean (only the new C05
authorization artifact + this evidence directory are
uncommitted; both are listed in `git status --short` after
the C1 RED commit is authored, not before).

Append-only invariant preserved: no amend, no rebase, no
force-push, no reset+recommit. C1 only documents defects;
no `.HC`, no `.sh`, no Makefile, no production source
mutation in this commit (per the C04 C1-RED precedent at
`229932d`).
