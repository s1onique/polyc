ACT-POLYC-BOOTSTRAP04 — C4 — residue
====================================

Per ACT §35 + §44.

------------------------------------------------------------
P0 blockers
------------------------------------------------------------

None. All B3 acceptance criteria pass at C4 capture time.

------------------------------------------------------------
P1 — important near-term work
------------------------------------------------------------

None within B3 scope.

The B3 ACT explicitly does NOT fix:

- unit/jit runner false-green (test infrastructure; runner
  state unchanged; classification: TEST_INFRASTRUCTURE;
  BLOCKS_B3: NO)
- B2 HANDOFF pointer typo (carried non-blocking governance
  residue; verbatim per ACT §4)
- B2 "same file by inode" over-wording (carried non-blocking
  governance residue; verbatim per ACT §4)

------------------------------------------------------------
P2 — deferred improvement
------------------------------------------------------------

- GEP01/push historical residue (carried; not widened)
- historical captured-evidence whitespace in src/x86.c and
  src/x86_64.c (carried; not modified by B3)
- token dump absence (carried)
- EOF-hygiene pre-commit hook
- Markdown section-order linter

All non-blocking per ACT §44 ("prose never blocks the
engineering board by itself").

------------------------------------------------------------
Unit/JIT classification (per ACT §35)
------------------------------------------------------------

```text
UNIT_TEST_CURRENT     = ENVIRONMENTALLY_UNAVAILABLE / RUNNER_FALSE_GREEN
UNIT_TEST_LAST_KNOWN  = 90/90
JIT_UNIT_TEST_CURRENT = ENVIRONMENTALLY_UNAVAILABLE / RUNNER_FALSE_GREEN
JIT_UNIT_TEST_LAST    = 90/90
ATTRIBUTABLE_B3_REGRESSION = NONE_OBSERVED

CLASS                 = TEST_INFRASTRUCTURE
PRODUCTION_IMPACT     = NONE
BLOCKS_B3             = NO
```

The B3 ACT did not modify the unit/jit runner. The
classification is mechanical, not invented.
