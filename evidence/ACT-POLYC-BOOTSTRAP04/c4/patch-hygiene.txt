ACT-POLYC-BOOTSTRAP04 — C4 — patch hygiene (re-run)
==================================================

Re-verified at C4 capture time.

```text
$ git diff --check 245da70..HEAD 2>&1
(empty)

$ git diff --check HEAD 2>&1
(empty)
```

Clean.

Historical whitespace in `src/x86.c` and `src/x86_64.c`
remains pre-existing and non-blocking (F14-frozen).
