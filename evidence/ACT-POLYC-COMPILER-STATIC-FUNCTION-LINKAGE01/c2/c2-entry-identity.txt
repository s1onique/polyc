ACT-POLYC-COMPILER-STATIC-FUNCTION-LINKAGE01 / c2 / c2-entry-identity.txt

C2 entry identity recorded:

```text
git rev-parse HEAD          = 1f24d6b (C1 CONTRACT/RED commit)
git branch --show-current   = main
git status --porcelain=v1   = modified: src/jit-common.c
```

C1 commit SHA: 1f24d6b
RED_PRIVATE_FNS_STALE_STATE found at C1 (see c1-red-summary.txt).

C2 minimal correction:
  src/jit-common.c           line ~575  +mapClear(jit->private_fns)
  src/jit-common.c           line ~908  +mapRelease(jit->private_fns)

No other production file modified.
