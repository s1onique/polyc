ACT-POLYC-COMPILER-STATIC-FUNCTION-LINKAGE01 / c3 / c3-patch-hygiene.txt

Patch hygiene verification.

  $ git diff --check 06c7951..HEAD
  (no output -- clean diff)

  AC33 requires: `git diff --check <ENTRY_HEAD>..<CANDIDATE_CLOSE>`
  be clean. ENTRY_HEAD = 06c7951 (HALT commit before this ACT).
  CANDIDATE_CLOSE = HEAD (current; C2 commit + staged C3 changes).
  Result: clean.

  PATCH_HYGIENE = PASS

  Historical HALT evidence whitespace outside this ACT's range
  (evidence/ACT-POLYC-SELFHOST-LEXER04/**) was NOT modified by
  this ACT -- F14 immutable predecessor surfaces preserved.
