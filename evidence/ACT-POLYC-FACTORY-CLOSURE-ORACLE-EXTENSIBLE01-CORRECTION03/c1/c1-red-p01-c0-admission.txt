ACT-POLYC-FACTORY-CLOSURE-ORACLE-EXTENSIBLE01-CORRECTION03 / c1 / c1-red-p01-c0-admission.txt

C1 RED witness for P0-G1: C0 pre-existing-work authorization.

The CORRECTION02 C0 ACT body (recorded at ca67d1a) contains the
admission in its Entry identity block:

  Entry identity:
    git branch --show-current = main
    git rev-parse HEAD        = b9a43f854ad8f82dd4c95bfabf6f71490f91e616
    git status --short        = clean (modulo unstaged whitespace fixes
                                  to CORRECTION01 evidence files; those
                                  are committed in C2 of this ACT)

The exact text recovered from git show ca67d1a
(ACT-POLYC-FACTORY-CLOSURE-ORACLE-EXTENSIBLE01-CORRECTION02.md
lines 14-19) is:

    git branch --show-current = main
    git rev-parse HEAD        = b9a43f854ad8f82dd4c95bfabf6f71490f91e616
    git status --short        = clean (modulo unstaged whitespace fixes
                                  to CORRECTION01 evidence files; those
                                  are committed in C2 of this ACT)

This text demonstrates:

This text demonstrates:

  - The ACT body's "git status --short" self-report is NOT clean.
  - The "modulo" clause enumerates substantive implementation work
    (whitespace fixes to evidence files) that was already present
    at C0 AUTH time.
  - That work was later formally attributed to C2 in CORRECTION02's
    phase sequence.

Per F15: a C0 ACT body must record authorization, not retroactively
authorize implementation work. The reverse chronology is a
self-authorization defect.
