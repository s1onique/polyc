ACT-POLYC-BOOTSTRAP01-CORRECTION01 — C4 — residue
==================================================

P0 (blocks the current/next decision): NONE.

P1 (important near-term work):

  R-P1-1  ACT-POLYC-BOOTSTRAP01 itself remains a
          Cardinality-1 historical exception. This
          correction enumerates EXCEPTION 4 but does
          not change the historical CLOSE commits.
          Recommended future ACT (non-blocking):
          ACT-POLYC-FACTORY-CLOSE-CARDINALITY01 to
          mechanically enforce Cardinality-1 (per
          DOCTRINE.md §24 last paragraph).

  R-P1-2  ACT-POLYC-BOOTSTRAP01 c4/closure-summary.txt
          still uses "N/A (path-dependent)" wording
          for B0_LLVM_AS and B0_LLVM_VERIFY. The
          *engineering* truth is now captured by this
          correction's c4/closure-summary.txt (the
          new one) as BLOCKED_BY_EXISTING_MEMORY01_
          FENCE. The predecessor's c4/closure-summary
          is F14 historical evidence and is not
          modified. A future maintenance ACT may
          choose to clarify the predecessor's wording
          via a CORRECTION02.

  R-P1-3  GEP01 cap-verifier (llvm-gep01-test) 26/4
          pre-existing failure split. Unchanged by
          this correction (and unchanged by B0).
          Inherited as NON_BLOCKING_GOVERNANCE_RESIDUE.

  R-P1-4  LLVM-backend IR_STORE_DEREF MEMORY01 fence.
          Path-dependent per Option-W C6. Unchanged
          by this correction.

  R-P1-5  Captured-evidence trailing whitespace at
          evidence/ACT-POLYC-BOOTSTRAP01/c3/
          fresh-build.txt:144 (DEFECT-3). Classified
          GOVERNANCE_RESIDUE; not blocking.

P2 (deferred improvement):

  R-P2-1  Pre-commit / pre-push hook to mechanically
          enforce EOF hygiene (1 newline max) on all
          new text files. Would have caught
          DEFECT-2 at the C2 IMPL step.

  R-P2-2  Markdown section-order linter that warns
          when numbered sections in an ACT document
          are out of order. Would have caught
          DEFECT-5 at authoring time.

End of residue classification.
