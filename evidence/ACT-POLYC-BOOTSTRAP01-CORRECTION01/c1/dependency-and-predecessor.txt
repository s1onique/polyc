ACT-POLYC-BOOTSTRAP01-CORRECTION01 C1 — dependency + predecessor
=================================================================

Predecessor ACT:

  ACT-POLYC-BOOTSTRAP01
  - opened at commit 6fcf810 (C1 RED)
  - IMPL at 4992870
  - EVIDENCE at 8618553
  - CLOSE at 4b42e06 (authoritative, per EXCEPTION 4)
  - docs-only follow-up at 147069f (incorrectly tagged
    CLOSE; should have been EVIDENCE)

Forward dependencies:

  This ACT does NOT depend on any subsequent ACT. It
  only depends on the historical ACT-POLYC-BOOTSTRAP01
  being observable in git history (it is).

  Downstream:
    ACT-POLYC-BOOTSTRAP02 (B1 partial self-host) is
    RECOMMENDED_NEXT_AFTER_THIS_CORRECTION_CLOSES.

No new dependency on GEP01, MEMORY01, or LLVM widening
is introduced. This ACT explicitly does NOT authorize
those work items.

External toolchain dependency:

  - hcc (production compiler) — used by make bootstrap01-test
    for native AOT path
  - cc  (host C compiler)    — used to compile the C oracle
  - git, make, bash, python3 — used by this ACT's evidence

  All available locally at this ACT's execution site.
