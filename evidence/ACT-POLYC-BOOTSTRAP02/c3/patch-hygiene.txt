ACT-POLYC-BOOTSTRAP02 — C3 — patch hygiene
============================================

Per ACT §23: `git diff --check <C3-entry>..HEAD`
classifies findings.

------------------------------------------------------------
Section A — Diff hygiene on C3 entry vs HEAD
------------------------------------------------------------

  $ git status --short
  ?? evidence/ACT-POLYC-BOOTSTRAP02/c3/
  ?? tools/quality/bootstrap02-corpus-runner.py
  ?? tools/quality/bootstrap02-error-corpus.py

  $ git add -N evidence/ACT-POLYC-BOOTSTRAP02/c3/ \
              tools/quality/bootstrap02-corpus-runner.py \
              tools/quality/bootstrap02-error-corpus.py

  $ git diff --check 6144361e HEAD
  (empty output)
  $ echo $?
  0

  $ git diff --check
  (empty output)
  $ echo $?
  0

No whitespace errors at EOF, no trailing whitespace,
no tab/space mixing in any of the C3-introduced files.

------------------------------------------------------------
Section B — Source/executable/config finding
------------------------------------------------------------

Source/executable/config finding introduced by C3:
NONE.

C3 introduced:
  - evidence/ACT-POLYC-BOOTSTRAP02/c3/*  (text evidence)
  - tools/quality/bootstrap02-corpus-runner.py
  - tools/quality/bootstrap02-error-corpus.py

The tools/quality/*.py files are TEST-ONLY drivers
that invoke ./hcc as a subprocess. They do not modify
production sources, build systems, or compiler
internals.

------------------------------------------------------------
Section C — Raw captured evidence (text only, post-fix)
------------------------------------------------------------

The C3 EVIDENCE commit on `de6b30c` initially committed
four generated Mach-O executables under
`evidence/ACT-POLYC-BOOTSTRAP02/c3/rebuild-A/` and
`rebuild-B/` (totalling ~2.9 MB). A bounded
forward-fix EVIDENCE commit deleted them; the
SHA-256 records are preserved as text in
`reproducibility.txt`:

  Build A hcc              = 98a687f7...8c461
  Build A bootstrap02-ident.o = a1b620c0...854f
  Build A hcc-bootstrap02  = 24d1408f...8a95d
  Build B hcc              = 98a687f7...8c461
  Build B bootstrap02-ident.o = a1b620c0...854f
  Build B hcc-bootstrap02  = 24d1408f...8a95d

No information is lost: hash equality is textually
provable from SHA-256 strings alone. The deletion
is a hygiene/bounded-payload improvement, not a
correction of the substantive proof.

C3 (post-fix) introduces ZERO binary artefacts under
the evidence tree. All evidence is text (ASCII or
UTF-8).

------------------------------------------------------------
Section D — Verdict
------------------------------------------------------------

AC-C3-40 new executable/source patch hygiene clean : PASS

No C3-introduced finding is BLOCKING.
No C3-introduced finding is GOVERNANCE.
The C3 forward-fix commit (next commit on `main`
after `de6b30c`, with trailers
`ACT: ACT-POLYC-BOOTSTRAP02` and
`ACT-Phase: EVIDENCE`) removes all four committed
generated binaries.
