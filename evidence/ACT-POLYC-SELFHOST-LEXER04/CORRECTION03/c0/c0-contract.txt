ACT-POLYC-SELFHOST-LEXER04-CORRECTION03 -- C0 -- architectural decision frozen
============================================================================

BOOTSTRAP_LINK_DIRECTIVE_ROLE = PRODUCTION_AUTHORITY

ABI model frozen at C0: ABI_MODEL = A (returned parsed-result adapter).

C0 commits with the original CORRECTION02 ABI 5 surface
(out_consumed, out_is_path, out_stored_len, out_error).

C1 recon revealed that ABI 5 is insufficient for production authority
because out_stored_len is a count but cannot identify which bytes
to copy. Therefore C1 freezes an ABI extension (additively):

  Added: U8 *out_target_bytes, I64 out_target_cap, I64 *out_target_len.

The C1 contract delta is at:
  evidence/ACT-POLYC-SELFHOST-LEXER04/CORRECTION03/c1/c1-c0-contract-delta.txt

C2 will implement both the C adapter and the ABI extension together.
