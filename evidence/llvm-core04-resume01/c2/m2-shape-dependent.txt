=== m2-shape-dependent.txt ===
SHAPE_DEPENDENT witness: src/tests/llvm-spike/red_local_multi_def.HC

IR_STORE (capability class SHAPE_DEPENDENT) is dispatched at the
seam in llLowerBlock (line ~950). Counter increments before any
shape-specific check, so the rejected multi-def path still
increments shape_dependent exactly once.

Captured stderr (rc=1, rejected):
LLVM backend capability contract: ok (56 rows, ordinal binding)
LLVM_BACKEND_UNSUPPORTED_SSA_LOCAL: function MultiDef: local id=9 has multiple reaching stores (this spike is SSA-only and does not insert phi nodes); line 22
CAPABILITY_COUNTERS supported=2 rejected=0 shape_dependent=4 defensive=0 unreachable=0
