=== m1-adversarial-permanent.txt (C3 EVIDENCE: post-IR_IADD renaming) ===
ACT-POLYC-LLVM-CORE04-RESUME01 C3 EVIDENCE: adversarial fixture
now uses real IrOp enum names (IR_IADD/IR_ISUB/IR_IMUL) so the
scope-discovery test exercises structural exclusion rather than
enum-name filtering.

  adversarial fixture functions reachable from llFunction(): []
  adversarial fixture rogue occurrence count: 3
  adversarial fixture location-aware rogue = none (helper not called from llFunction())

Permanent adversarial fixture rejection (verifier):
PASS  scope-tight self-test (RESUME01 AC13): permanent adversarial fixture rejected by unified scope; unified model is internally consistent; no rogue `if`-arms outside discovered scope; strong NC1 (already-supported IR_IADD in non-dispatch helper) is correctly detected by the location-aware check.
