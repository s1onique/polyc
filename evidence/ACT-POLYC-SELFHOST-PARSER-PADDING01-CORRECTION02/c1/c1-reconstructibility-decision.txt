ACT=ACT-POLYC-SELFHOST-PARSER-PADDING01-CORRECTION02
PHASE=C1-RECON

# C1 §9.2 reconstructibility classification.
#
# Decision is single-valued: NO.
# This decision is REQUIRED by §10 (C1 branch decision).

HISTORICAL_BASELINE_RECONSTRUCTIBLE=NO

# Justification (mechanical, evidence-bound):
#
# 1. The 9 stage0-historical .o files are not in any git object.
#    Verified: git rev-list --objects --all and
#    git rev-list --objects --all --unreachable both return
#    0 hits for build/b02-corpus-A/*.o across all 9 expected
#    filenames. See c1-baseline-input-availability.tsv.
#
# 2. The 9 stage0-historical .o files are not generated by any
#    committed build recipe. Verified: grep on Makefile,
#    scripts/quality/*.sh, tools/quality/*.HC for "b02-corpus-A"
#    yields only READ references (the S0 fallback path in
#    tools/quality/lexer07-broad-corpus-4-stage.HC and the
#    expected-SHA verification in tools/quality/lexer07-proof-verify.HC);
#    no WRITE or PRODUCE references. See c1-baseline-input-availability.tsv.
#
# 3. The only committed artifacts of the historical baseline are
#    the 9 SHA-256 digests, preserved in:
#      - evidence/ACT-POLYC-SELFHOST-LEXER02/CORRECTION05-CORRECTION01/c3/corpus-object-provenance.tsv
#      - tools/quality/lexer07-proof-verify.HC (compiled-in BASELINE_SHA[9])
#    SHA-256 is a one-way function. Digests alone do not enable
#    byte reconstruction.
#
# 4. The predecessor CORRECTION01 C3 evidence
#    (evidence/ACT-POLYC-SELFHOST-PARSER-PADDING01-CORRECTION01/c3/c3-lexer07-delta.txt)
#    reached the same conclusion:
#      "The historical fallback path is disabled because
#       build/b02-corpus-A/ has not been regenerated since the
#       LEXER02-CORRECTION05-CORRECTION01 baseline. This is a
#       test infrastructure drift, not a parser-padding regression."
#
# 5. The FACTORY-CLOSURE-STATUS surface also observed the same
#    (evidence/ACT-POLYC-SELFHOST-SURFACE-RECON03/c1/c1-broad-corpus-baseline.txt):
#      PASS_S0=166 HISTORICAL_S0=0 PASS_S1=175 PASS_S2=175 PASS_S3=175
#      HISTORICAL_S0_FALLBACKS_USED=0

# Lock 3 of the ACT (F-DOCTRINE) explicitly forbids treating
# "build/ disappearance" as a benign explanation; reconstruction
# must be derived from committed authority. No committed authority
# contains the bytes.

# Branch decision per §10:
ACTIVE_BRANCH=B
