C7 ROADMAP update (descriptive pointer)
========================================

This file is descriptive only; the authoritative ROADMAP
edit lives in the C7 commit itself. C7 modifies
docs/ROADMAP.md to:

  1. Append a CLOSED status block under the existing
     "ACT-POLYC-LLVM-LOCAL-MEM2REG01-CORRECTION02 status
     (RED; contract opened; IMPL NOT authorised)" section,
     promoted to "CLOSED" with the full lineage.

  2. Record the final production architecture rather than
     the original planned commit count.

  3. Mark C4 v1 = SUPERSEDED by C4 v2.

  4. Cite the C7 evidence dir path
     (evidence/.../c7/) and the C6.3 frozen artefacts
     (c6.3/six-fixture-matrix.real.txt, c6.3/boundary-
     interpretation.real.txt, c6.3/validate.real.txt).

  5. NOT mutate the immutable historical C6.1 evidence.

The C7 commit trailer carries the canonical Factory v2
verdict identity:

  ACT: ACT-POLYC-LLVM-LOCAL-MEM2REG01-CORRECTION02
  ACT-Phase: CLOSE
  ACT-Verdict: PASS

(plus optional ACT-Supersedes pointing at the C6.3
entry commit f7060a9 if the trailer grammar requires it;
the trailer is part of the commit message, not a
mutable authoritative `Status:` field on the ACT
document.)
