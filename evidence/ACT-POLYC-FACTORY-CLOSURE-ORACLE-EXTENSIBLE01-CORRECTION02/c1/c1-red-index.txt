ACT-POLYC-FACTORY-CLOSURE-ORACLE-EXTENSIBLE01-CORRECTION02 / c1 / c1-red-index.txt

C1 RED index for CORRECTION02.

P0-1 / F-POLYC-TOOLS substantive logic in shell:
  c1-red-p01-polyc-engine.txt
  Red confirmed: 49-LOC shell engine holds expect(), fixture
  construction, checker invocation, RC + grep matching,
  PASS/FAIL aggregation; PolyC binary is a thin driver.

P0-2 / Phase binding:
  c1-red-p02-monotonic-rebuild.txt
  Red confirmed: C3 was not its own committed boundary;
  CORRECTION01 evidence files were committed inside the C2b
  commit; the phase binding is constructed on C2b+C4 only.

P0-3 / Patch hygiene:
  c1-red-p03-whitespace.txt
  Red confirmed: 4 CORRECTION01 evidence files have trailing
  blank lines that git diff --check flags.
