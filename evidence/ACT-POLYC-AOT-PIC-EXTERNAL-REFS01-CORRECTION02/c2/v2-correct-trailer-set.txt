=== v2-correct trailer set for ACT-POLYC-AOT-PIC-EXTERNAL-REFS01-CORRECTION01 CLOSE ===

This is the trailer set that SHOULD have been committed at 505b007.
It is captured here as a plain-text artifact (not as a trailer
modification of 505b007, which would be forbidden by F-GIT-IDENTITY).

-- v2-correct trailer set --
ACT: ACT-POLYC-AOT-PIC-EXTERNAL-REFS01-CORRECTION01
ACT-Phase: CLOSE
ACT-Verdict: PASS
ACT-Supersedes: ACT-POLYC-AOT-PIC-EXTERNAL-REFS01
ACT-Corrected-Verdict: HALT_GATE_PUSH_FAILED

-- v2 section 2.3 example for the precedent pattern --

```
ACT: ACT-POLYC-LLVM-CORE04-CORRECTION01
ACT-Phase: CLOSE
ACT-Verdict: PASS
ACT-Supersedes: ACT-POLYC-LLVM-CORE04
ACT-Corrected-Verdict: HALT_RED_NOT_REPRODUCED
```

Interpretation:

* the correction ACT itself PASSES;
* the predecessor's historical closure is corrected to
  `HALT_RED_NOT_REPRODUCED`;
* no HANDOFF rewrite is needed.


-- HALT classification binding surface (descriptive only) --
  - evidence/ACT-POLYC-AOT-PIC-EXTERNAL-REFS01-CORRECTION01/c4/closure-summary.txt
    PRE-CONDITION CLAIM block:
PRE-CONDITION CLAIM (the predecessor ACT under correction)
  production_semantic_delta = GREEN
  gate_push_failure         = FAIL (gep01 only)
  gate_push_failure_attributable_to_production_delta = FALSE
  v2 classification         = GOVERNANCE / BLOCKS_NEXT=NO


  - docs/ROADMAP.md CORRECTION01 status block (HALT_CLASS/BLOCKS_NEXT in prose):
                              HALT_CLASS: GOVERNANCE
                              BLOCKS_NEXT: NO)

  CLOSURE VERDICT (v2 doctrine):
--
    HALT_CLASS               : GOVERNANCE
    BLOCKS_NEXT              : NO

  MISSION = reconcile the closure-truth gap of the predecessor
--
             the changed production subject -> HALT_CLASS=
             GOVERNANCE, BLOCKS_NEXT=NO").

  PRODUCTION CHANGE = zero bytes (this ACT does not re-mutate
--
  BLOCKS_NEXT = NO  (per v2 §25 HALT_CLASSIFICATION + CORRECTIONS,
                     this is the documented board signal that the
                     roadmap may proceed for a same-scope

-- v2-correct set passes the verifier --
MODE=ACT
STATUS=PASS
ACT=ACT-POLYC-AOT-PIC-EXTERNAL-REFS01-CORRECTION01
PHASE=CLOSE
VERDICT=PASS
HALT_CLASS=
BLOCKS_NEXT=
