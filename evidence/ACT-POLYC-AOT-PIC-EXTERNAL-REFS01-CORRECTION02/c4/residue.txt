ACT-POLYC-AOT-PIC-EXTERNAL-REFS01-CORRECTION02 — Residue
=========================================================

P0 = none

P1 = none

P2 = one item: the historical commit `505b007` (C4 CLOSE of
     CORRECTION01) carries a trailer block that violates
     v2 §2.4 + missing ACT-Supersedes. Per F-GIT-IDENTITY
     and `docs/factory/DOCTRINE.md` §23, the historical
     commit is immutable; the v2-correct trailer set is
     captured as a plain-text artifact under `c2/` and
     the HALT classification binding is in the descriptive
     artifacts. This is the only mechanical residue and
     it is non-blocking (corrected verdict on a
     grandfathered predecessor; classification is
     descriptive).
