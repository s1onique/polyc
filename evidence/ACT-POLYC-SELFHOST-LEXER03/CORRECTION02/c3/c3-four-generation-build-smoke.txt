ACT-POLYC-SELFHOST-LEXER03-CORRECTION02 -- C3 EVIDENCE -- 4-gen build smoke
==========================================================================

Per ACT §16: re-run the LEXER03 broad-corpus 4-stage smoke test (which
exists independently of the new fixed-point evidence). This is the
"build/link conservation" gate; the new byte-equality proof is a
different, stronger gate.

COMMAND (literal):
  make lexer08-broad-corpus-4-stage

OBSERVED:
  LEXER08_BROAD_CORPUS_4_STAGE=PASS

INTERPRETATION:
  FOUR_GENERATION_BUILD_SMOKE = PASS

AC20  LEXER08_BROAD_CORPUS_4_STAGE = PASS   (PASS)

This is a smoke-only gate (it echoes PASS once all four compiler
binaries exist). The strong four-generation fixed-point gate is the
new fixed-point proof (see c3-byte-equality.txt + c3-sha256-equality.txt).
