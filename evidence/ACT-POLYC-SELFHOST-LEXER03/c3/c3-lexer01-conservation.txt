ACT-POLYC-SELFHOST-LEXER03 -- C3 -- LEXER01 (operator) conservation
====================================================================

PROBE:  The LEXER03 implementation does not regress the LEXER01
        (operator/punctuation recognizer) which is already PolyC-
        selfhosted via BootstrapClassifyOperator.

CHECKS:
  1. LEXER01 direct differential: 47/47 PASS
     Command: make bootstrap06-direct-differential
     Result: BOOTSTRAP06_DIRECT_DIFFERENTIAL=PASS

  2. LEXER01 component object exports BootstrapClassifyOperator:
     $ nm ./build/bootstrap06-operator-classify.o | grep Bootstrap
     U _BootstrapClassifyOperator
     (referenced by lexer.c.o stage1)

  3. Stage1 lexCore case for operators still calls into the
     BootstrapClassifyOperator ABI without modification. The
     lexer.c changes for LEXER03 are limited to:
       - lexSkipCodeComment (trivia)
       - lexCore whitespace cases ' ', '\t', '\n', '\r' (trivia)
     Operator cases ('+', '-', '*', '/', '%', '&', '|', '^',
     '!', '~', '<', '>', '=') are unchanged.

VERDICT: PASS

  No regression observed in LEXER01. The 47-fixture differential
  still passes 47/47 after LEXER03 is added to the link topology.

NOTE: bootstrap06-lexer-seam-stage1 (the production-seam runner
for LEXER01) currently fails to link due to a pre-existing
test-prefix-install issue (hcc -lib asm error in src/holyc-lib/
strings.HC:364). This failure is reproducible on the prior commit
(verified by `git stash` + `make bootstrap06-lexer-seam-stage1`),
so it is not caused by LEXER03. Tracked as a separate pre-existing
issue (P1 residue for the BOOTSTRAP02/Bootstrap06 chain).
