ACT-POLYC-FACTORY-NO-SHA-OF-SELF01-CORRECTION02 — C05 doctrine classification

=== HISTORICAL TEXT IN C05 ===

The file
`evidence/ACT-POLYC-TOOLING-RUNTIME01/correction05/f14-reconciliation.txt`
contains this passage:

  > === THE PROPER PATTERN (going forward) ===
  >
  > When a future correction discovers an evidence defect in an
  > earlier packet:
  >
  >   1. Append a clearly-marked "ADDENDUM" block to the older
  >      file (as c04 did for patch-hygiene.txt) — this is the
  >      minimum-impact option.
  >   2. OR: create a new correction directory with the corrected
  >      version and link it from a master registry.
  >   3. NEVER silently rewrite.

This text was written by the closing agent for CORRECTION05
before the strict F14 reading was codified in DOCTRINE.md §22.

=== CLASSIFICATION (per this correction) ===

The C05 text listed TWO acceptable patterns ("append ADDENDUM"
or "create new correction directory"). The current DOCTRINE.md
§22 strict F14 reading forbids pattern (1) and permits only
pattern (2). The C05 text is therefore SUPERSEDED historical
guidance, not current Factory doctrine.

This classification does NOT modify the C05 file. Under F14,
the C05 evidence directory is immutable historical evidence,
and re-mutating it (whether to add a note, correct wording,
or remove the now-superseded passage) would itself be an F14
violation.

The authoritative current doctrine is:

  DOCTRINE.md §22 "Strict F14 reading for corrections":
    historical erroneous evidence = immutable
    new correction packet         = authoritative successor
    [corrections] MUST NOT modify any file under ACT-N/.

The C05 "ADDENDUM is acceptable" passage remains in the C05
file as historical evidence of an earlier (now-superseded)
interpretation. Future consumers reading C05 should consult
this classification file for the current interpretation.

=== FILE STATE ===

  evidence/ACT-POLYC-TOOLING-RUNTIME01/correction05/f14-reconciliation.txt
    Status: UNCHANGED from CORRECTION05 (F14 immutable)
    Contains: superseded "ADDENDUM is acceptable" guidance
    Effective doctrine: see DOCTRINE.md §22

This classification is the successor-packet pattern from
DOCTRINE.md §22 working as designed: rather than mutating
the historical file to "remove" the superseded text, we
classify it from a new packet that future readers can find.
