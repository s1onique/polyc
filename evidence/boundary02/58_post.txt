# 58_struct_return.HC — post-fix targeted witness
#
# ACT-POLYC-IR-BOUNDARY02-REQUAL01.
#
# Reducer from original IR-BOUNDARY02 RED:
#   ./hcc ... -S src/tests/58_struct_return.HC
#   ERROR: ir-regalloc: no slot for var.id=65 kind=4
#
# Post-fix (commit 404644d, qualified by ACT-POLYC-FACTORY-PUSH-HERMETIC01):
test=58_struct_return.HC
target=aarch64-apple-darwin
install-dir=build/hermetic-prefix
command=../../hcc --target=aarch64-apple-darwin --install-dir=$PWD/build/hermetic-prefix 58_struct_return.HC -o /tmp/58.s
exit-status=0
output-bytes=35448
canonical-gate=scripts/quality/gate-push.sh 404644d
canonical-gate-line=CHECK=aot STATUS=PASS

# Diagnostic detail: the hidden-out-pointer arrive (IR_PARAM_KIND_HIDDEN_SRET)
# is now pushed into fn->params, so the post-pass stamps it with the
# platform's sret register. Body then uses the stamped arrive, not an
# unslotted arrive.
