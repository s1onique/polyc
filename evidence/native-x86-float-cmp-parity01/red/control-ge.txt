AC16 - GE (>=) NaN control (must remain GREEN)

Source-level observable: NaN >= 1.0 should be 0. Current: 0. CORRECT.

Evidence: evidence/native-x86-float-cmp-parity01/red/red-nan-aot.txt:
  NaN >= 1.0  -> 0   (expected 0)
  1.0 >= NaN  -> 0   (expected 0)
  NaN >= NaN  -> 0   (expected 0)

Assembly witness (BEFORE):

  ucomisd %xmm1, %xmm0
  setae   %al

On UCOMISD NaN, flags are ZF=PF=CF=1. setae (above-or-equal) requires
CF=0; CF=1 fails, so setae returns 0. Coincidentally correct.

The fix must NOT alter this code path. AC24 binds it.

AC16 reproducer: PASS (live x86_64 hcc).
