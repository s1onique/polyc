ACT-POLYC-LLVM-OPTION-W-OUT-PARAM01 C3.5 RED: producer-binding evidence + identity preservation=========================================================================================QUESTION (per expert review of C3.4):  C3.4 froze the producer-opcode binding doctrine but had  TWO P0 defects that should be fixed before production code  is written:  P0-1  The C3.4 witness-leak had an INVERTED predicate. Its        `leak` variable measured "witness matches its own        hard-coded expected table" rather than "current        discriminator leaks an unauthorized producer". A        witness that always returns its expected values is a        tautology, not a leak.  P0-2  The C3.4 pseudocode is ambiguous about the domain of        `v` in the i1 -> i8 normalization loop. The prose        says "v is the previously-lowered incoming value"        (an LLVMValueRef), but then calls irVarId(v) to        index the def_map (which expects an IrValue *).        The neutral-vs-LLVM identity boundary must be        preserved.  Plus a P1 sharpening:  P1    The LLDefMap construction currently records every        instruction with a `dst`. For IR_VAL_TMP (SSA-        unique), this should be UNIQUE: a duplicate        definition for an already-bound id must halt with        HALT_PHI_TYPE_CONTRACT_REQUIRED rather than silently        overwriting.MECHANICAL WITNESS (P0-1 fix)  evidence/c3.5/witness-leak.c    183 lines; no LLVM linkage                                   (corrected leak predicate)  evidence/c3.5/witness-leak.txt  captured output  evidence/c3.5/BUILD.txt         build commands; no binaries  Required matrix (4 cases):    case                  curr     producer_authorized  leak?    --------------------   -------  -------------------  ------    phi_icmp_producer     ACCEPT   YES                  NO    phi_fcmp_producer     ACCEPT   NO                   YES    phi_param_i1          ACCEPT   NO                   YES    phi_no_zext_i8        direct   (not normalised)     NO  LEAK_ROWS must equal 2 (FCMP + PARAM).  Process must exit 0 ONLY if the observed matrix is EXACTLY  as above.  The corrected predicate is:    leak(curr, producer) =        curr_accepts AND NOT producer_authorized    producer_authorized =        has_producer AND producer_op == IR_ICMP  This is independent of the witness's own expectations: the  leak exists iff the current discriminator accepts a value  whose producer is NOT authorized by the §9.4 contract.  CAPTURED OUTPUT (literal):    case                   width   prod    expected  observed  corrected leak?    ---------------------- ------- ------- --------- --------- --------- -------    phi_icmp_producer      1       IR_ICMP ACCEPT    ACCEPT    ACCEPT    no    phi_fcmp_producer      1       IR_FCMP ACCEPT    ACCEPT    reject    YES    phi_param_i1           1       (none)  ACCEPT    ACCEPT    reject    YES    phi_no_zext_i8         8       (none)  reject    reject    reject    no    Leak rows (curr accepts AND !producer_authorized): 2    Required leak rows:                                2    Matrix matches required shape:                     YES    OUTCOME_F_PRODUCER_LEAK_CONFIRMED  Compare to C3.4 witness-leak output:    Leak rows: 0    OUTCOME_F_PRODUCER_LEAK_CONFIRMED  C3.4's witness was internally contradictory: "Leak rows: 0"  but "OUTCOME_F_PRODUCER_LEAK_CONFIRMED". C3.5's witness has  the expected geometry: LEAK_ROWS=2 with the verdict  determined by the OBSERVED matrix, not by tautological  self-consistency.CORRECTED PSEUDOCODE (P0-2 fix; §9.5 algorithm)  The C3.4 pseudocode used a single `v` variable whose domain  was ambiguous between IrValue * and LLVMValueRef. C3.5  freezes the identity boundary.  case IR_PHI:      validate bounded short-circuit shape      validate exact predecessor set      /* Option A: counter once per SHAPE admission, at top */      LL_INC_SHAPE_DEPENDENT(lc);      phi = LLVMBuildPhi(lc->bld,                         LLVMInt8TypeInContext(lc->ctx),                         "phi")      for each incoming pair (ir_in, ir_pred):          /* TWO identities, always kept distinct */          IrValue     *ir_in    = pair->ir_value;     /* NEUTRAL */          LLVMValueRef llvm_in  = llLowerValue(lc, ir_in);  /* LLVM */          /* §9.3.1: lookup-only predicate, NOT get-or-create */          LLVMBasicBlockRef pred_bb =              llbmGet(&lc->blocks, ir_pred->id);          if (!pred_bb)              HALT_PHI_EDGE_MATERIALIZATION_REQUIRED;          /* ensure pred_bb is actual CFG predecessor */          assert_in_predecessor_set(pred_bb);          /* discriminator (TYPE check on LLVM, PRODUCER             check on NEUTRAL) */          if (LLVMTypeOf(llvm_in) ==              LLVMInt8TypeInContext(lc->ctx)) {              /* i8 incoming: use directly */              v = llvm_in;          }          else if (LLVMTypeOf(llvm_in) ==                   LLVMInt1TypeInContext(lc->ctx)) {              /* i1 incoming: producer MUST be IR_ICMP */              IrInstr *producer =                  lldmGet(&lc->def_map, irVarId(ir_in));              if (!producer ||                  producer->op != IR_ICMP ||                  producer->dst != ir_in) {                  HALT_PHI_TYPE_CONTRACT_REQUIRED;              }              /* PI-2 / PI-3 / build-position discipline                 (unchanged from C3.4) */              LLVMValueRef pred_term =                  LLVMGetBasicBlockTerminator(pred_bb);              if (!pred_term)                  HALT_PHI_EDGE_MATERIALIZATION_REQUIRED;              assert_instruction_in_block_before(                  producer, pred_bb, pred_term);              LLVMBasicBlockRef save =                  LLVMGetInsertBlock(lc->bld);              LLVMPositionBuilderBefore(lc->bld, pred_term);              v = LLVMBuildZExt(lc->bld, llvm_in,                    LLVMInt8TypeInContext(lc->ctx),                    "phi_icmp_zext");              LLVMPositionBuilderAtEnd(lc->bld, save);          }          else {              HALT_PHI_TYPE_CONTRACT_REQUIRED;          }          add (v, pred_bb) to PHI incoming list      cache PHI dst  Two identities are now explicit:    ir_in    : IrValue *      (NEUTRAL IR; used for                                def_map lookup, producer->dst                                comparison, source of truth)    llvm_in  : LLVMValueRef   (LLVM IR; used for type check,                                zext emission, PHI incoming                                value)  The def_map MUST be indexed by irVarId(ir_in), NEVER by any  derived property of llvm_in. The producer->dst comparison  uses ir_in (the IrValue pointer), never a re-extracted id  from the LLVMValueRef.UNIQUE-DEFINITION CONTRACT (P1 fix)  C3.4 §9.4.1 said:    listForEach(b->instructions) {        IrInstr *ins = (IrInstr *)it->value;        if (ins->dst) {            lldmSet(&lc->def_map, irVarId(ins->dst), ins);        }    }  This silently overwrites a prior definition with the same  IrValue id. For IR_VAL_TMP (the bounded short-circuit  geometry), the neutral IR has SSA-uniqueness: each tmp is  defined exactly once. Silently overwriting a def_map entry  defeats the producer provenance check.  C3.5 freezes the unique-definition contract:    static void lldmSet(LLCtx *lc, u32 id, IrInstr *ins) {        IrInstr *old = lldmGet(&lc->def_map, id);        if (old && old != ins) {            HALT_PHI_TYPE_CONTRACT_REQUIRED                  "duplicate definition for IrValue id %u: "                  "old=%p, new=%p. IR_VAL_TMP must have a "                  "unique defining instruction.",                  id, (void *)old, (void *)ins);        }        lldmGrow(&lc->def_map, id + 1);        lc->def_map.values[id] = ins;    }  The map's meaning is sharpened from "the last instruction  encountered with that destination id" to "the UNIQUE  defining instruction for that IrValue id".  This makes producer provenance security-boundary-like  compiler evidence: any violation is detected at def_map  construction time, not at PHI-lowering time.SCOPE (NO EXPANSION)  - No new diagnostic macros (HALT_PHI_TYPE_CONTRACT_REQUIRED    pre-exists).  - No change to Gate 3 / Gate 4 / Gate 5 logic.  - No change to llType, llLowerValue, llCreateBlocks.  - One new bounded helper variant (lldmSet with uniqueness    check); the existing lldmSet is REPLACED (not augmented).  - The witness-leak predicate is corrected in-place; no new    witness required.  - No committed witness binaries (P2 hygiene per expert).NEW ACCEPTANCE CRITERIA (AC57-AC60)  AC57 C3.5 freezes the corrected leak predicate:       leak(curr, producer) =           curr_accepts AND NOT producer_authorized       where producer_authorized =           has_producer AND producer_op == IR_ICMP       The C4 verification harness MUST use this predicate       (or an equivalent direct semantic check), not a       self-consistency check against hard-coded expectations.  AC58 C3.5 freezes the required leak matrix:       ICMP    leak=NO       FCMP    leak=YES       PARAM   leak=YES       I8      leak=NO       LEAK_ROWS must equal 2. The harness exits 0 ONLY       if the matrix is observed exactly.  AC59 C3.5 freezes the identity-preservation contract for       the i1 -> i8 normalization path:         IrValue     *ir_in   = pair->ir_value;         LLVMValueRef llvm_in = llLowerValue(lc, ir_in);       The def_map lookup MUST use irVarId(ir_in). The       producer->dst comparison MUST use ir_in (the       IrValue pointer), not any derived id from llvm_in.  AC60 C3.5 freezes the unique-definition contract for the       def_map construction pass: a duplicate definition       for an already-bound IrValue id MUST halt with       HALT_PHI_TYPE_CONTRACT_REQUIRED. The map's meaning       is sharpened to "the UNIQUE defining instruction       for that IrValue id".HISTORICAL C4_IMPL_B_AUTH TABLE (C3.5 freeze, append-only)  C3       AUTH=true    SUPERSEDED   (pre-dominance)  C3.1     AUTH=true    SUPERSEDED   (pre-placement)  C3.2     AUTH=false   SUPERSEDED   (pre-mechanics)  C3.3     AUTH=true    SUPERSEDED   (pre-producer-binding)  C3.4     AUTH=true    SUPERSEDED   (pre-evidence+identity)  C3.5     AUTH=true    CURRENT      (P1-P7 all closed)PRE-C4 contract requirements (C3.5 freeze, all closed):  P1 contract frozen                         CLOSED C3.3  P2 counter semantics frozen                CLOSED C3.3  P3 lookup-only PI-1 mechanism identified   CLOSED C3.3  P4 implementation scope frozen             CLOSED C3.3  P5 producer-opcode binding frozen          CLOSED C3.4  P6 counter-vs-halting semantic frozen      CLOSED C3.4  P7 leak-predicate + identity preservation  CLOSED C3.5       (corrected predicate + ir_in/llvm_in        boundary + unique-definition contract)  All P1-P7 are CLOSED. C4 is READY.IDENTITY  ENTRY_HEAD      = 7875da7655c525e261aabe9975bfe3a838a96238  C3.5_RED        = <this commit>  WORKTREE        = clean (after commit)PRODUCTION DELTA: NONE (C3.5 is RED evidence + contract                     correction only).EVIDENCE (this commit)  c3.5/c3.5-red.txt            contract correction + pseudocode  c3.5/witness-leak.c          corrected leak predicate witness  c3.5/witness-leak.txt        captured output (LEAK_ROWS=2)  c3.5/BUILD.txt               build commands; no binariesRESIDUE  P0  none (the two P0 evidence/contract defects are frozen)  P1  (historical) 0fcc8b2 hygiene commit was a F14 pattern      repetition; not repeated in this commit  P2  (carried) C3.4 also did not commit binaries; C3.5      continues this convention.