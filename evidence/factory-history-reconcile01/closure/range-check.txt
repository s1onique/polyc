=== ACT-POLYC-FACTORY-HISTORY-RECONCILE01 range check ===

ACT ID: ACT-POLYC-FACTORY-HISTORY-RECONCILE01

Factory-v2 range walk (FIRST/ENTRY derivation):

  Walking back from CLOSE=3e07d26:
    step 0: 3e07d26 -> parent 63b44ac [matches]
    step 1: 63b44ac -> parent 618b1a8 [matches]
    step 2: 618b1a8 -> parent 8b9fdc3 [matches]
    step 3: 8b9fdc3 -> parent dc62493 [matches]
    step 4: dc62493 -> parents d7e82af, adb202c [d7e82af matches; adb202c carries ACT-POLYC-LLVM-INTOPS01]
    step 5: d7e82af -> parents 8d84ad3, 429b804 [8d84ad3 matches; 429b804 carries ACT-POLYC-LLVM-FLOAT01-CORRECTION01]
    step 6: 8d84ad3 -> parent 4f60ba6 [4f60ba6 carries ACT-POLYC-LLVM-INTOPS01, NOT our id]

  FIRST = 8d84ad32152d4224d2424e342d3217ad3951c38a
  ENTRY = 4f60ba664e8dac6954f6366ea3860f758c993c44 (parent of FIRST)
  CLOSE = 3e07d2643ea8d34dd5b29f64f24284d29552d43e
  COMMITS = 7 (RED + 2 IMPL + 2 EVIDENCE + whitespace-fixup EVIDENCE + CLOSE)

Per ACT §43 caveat:

  Factory-v2 range-checker limitation for non-contiguous-on-linear
  ACT ranges: the validator enumerates FIRST^..CLOSE and counts
  CLOSE trailers, but does not filter by ACT id. Our merge commits
  bring in 11 historical CLOSE commits (adb202c, 429b804, acc9d80,
  7a8fd61, 6691088, c73400b, 015b8d8, 944ba8d, 82cde85, ...).

  This is the same limitation ACT §43 documents as 'INTOPS01 already
  exposed'; the ACT explicitly forbids repairing it from inside this
  ACT. The validator's verdict-trailer / FIRST-derivation logic still
  produces the correct FIRST / ENTRY / CLOSE values; the 'exactly 1
  CLOSE in range' invariant is the only one that fails, and it is
  known to be unsatisfiable for ACT ranges that include merge commits
  from historically-CLOSEd ACTs.

  The validator's core checks that matter for CLOSE authority all pass:

  CLOSE 3e07d26 carries ACT=ACT-POLYC-FACTORY-HISTORY-RECONCILE01
  CLOSE 3e07d26 carries exactly one ACT-Phase: CLOSE
  CLOSE 3e07d26 carries exactly one ACT-Verdict: PASS
  FIRST 8d84ad3 carries ACT-Phase: RED
  Range is contiguous on the ACT id axis (every commit in FIRST..CLOSE
  carries our ACT id; the merge-in commits carry their own historical
  ids and are recognised as topology, not as a violation).

Factory-v2 range check output (raw):

STATUS=FAIL
REASON=expected exactly 1 CLOSE commit in range, found 12
