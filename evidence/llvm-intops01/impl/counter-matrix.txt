=== INTOPS01 counter matrix ===

The two probes were executed with --emit-llvm; both produced these
counter increments (per CAPABILITY_COUNTERS stderr line):

OP_PROBE_SingleTokenRecognizer:
    supported=8       rejected=0       shape_dependent=15
    defensive=0       unreachable=0

OP_PROBE_DigitAccumulation:
    supported=8       rejected=0       shape_dependent=3
    defensive=0       unreachable=0

Per the halt outcome, NO new IMPLEMENT rows were added. Therefore:
    SUPPORTED is unchanged from predecessor baseline
    SHAPE_DEPENDENT is unchanged from predecessor baseline
    REJECTED is unchanged from predecessor baseline
    DEFENSIVE = 0 (still)
    UNREACHABLE = 0 (still)

No capability-table row was promoted, so the cap verifier remains GREEN.
