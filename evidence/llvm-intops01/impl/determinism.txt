=== INTOPS01 determinism check ===

Both probes were emitted twice; the resulting .ll was byte-identical.

OP_PROBE_SingleTokenRecognizer:
    run1: b0_probe.ll
    run2: b0_probe.ll (byte-identical)

OP_PROBE_DigitAccumulation:
    run1: b0_digit_accum.ll
    run2: b0_digit_accum.ll (byte-identical)

No timestamps, pointer addresses, or nondeterministic counter text
appears in the .ll files. CAPABILITY_COUNTERS lines are stderr-only.
