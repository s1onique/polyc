# PRE-C0 EVIDENCE TREE

This directory contains evidence created **before the ACT body was
corrected to its final form**. It was originally placed in `c0/` but
that mis-classification is corrected here:

- `pre-c0-entry-identity-snapshot.txt`  (originally `c0/c0-entry-identity.txt`)
- `pre-c0-defect-decomposition-snapshot.md` (originally `c0/c0-defect-decomposition.md`)

Both files were committed at `b73fe6b` while the ACT body still
frozen `ENTRY_HEAD = 8393d4d`. Since the ACT body itself was
incorrect, no file produced under that ACT body can serve as C0
evidence. They are preserved here for full traceability of the
revision history but **do not satisfy any CORRECTION01 acceptance
criterion**. The real C0 evidence will be produced under the
corrected ACT body, in commit `b73fe6b..<C0 commit>`.

DELEGATE01 evidence is unchanged by all of this (F14-respected).
